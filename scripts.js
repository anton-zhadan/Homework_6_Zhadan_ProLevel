a = +prompt(); 
b = +prompt(); 
h = +prompt(); 
sumOfFactorials = 0;
for(; a<=b; a+=h) {

    for(j=1; j<=a; j++) {
        if(a%j === 0) {   
        }
    }
    factotial = 1;

    for (i=1; i<=j; i++) {
        factotial = factotial * i;  
    }
    sumOfFactorials += factotial;
}

console.log (sumOfFactorials += factotial);